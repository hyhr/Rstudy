elasticsearch:
  server:
    # es 默认最大条数
    max-size: 100
    max-conn-total: 100
    max-conn-per-route: 100
    # es 节点信息
    hosts:
      - hostname: localhost
        port: 9200
        schemeName: http
